export class burnDownGenerator {
  burnDown(sprint, sprintTickets) {}

  separateStatusBasedTicket(sprintTickets) {
		let sortedTickets = {};
		sprintTickets.forEach(element => {

		});
    // seperate the tickets based on the status of the ticket
    // output to graph generator
  }

  graphGenerator() {
    // genetate graph for burndown tickets based on status
  }
}
